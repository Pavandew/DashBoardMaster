<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color when selected (White for contrast on gradient) -->
    <item android:color="@color/black" android:state_checked="true" />
    <!-- Color when NOT selected (Semi-transparent white) -->
    <item android:color="@color/de_selected" android:state_checked="false" />
</selector>